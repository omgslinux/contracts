{% extends 'base.html.twig' %}

{% block body %}
    <h1>activePrinciple</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activePrinciple.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ activePrinciple.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('activePrinciple_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('activePrinciple_edit', { 'id': activePrinciple.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
