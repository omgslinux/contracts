{% extends 'base.html.twig' %}

{% block body %}
    <h1>Contractdescription</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contractDescription.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ contractDescription.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('contractdescription_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('contractdescription_edit', { 'id': contractDescription.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
